from distutils.core import setup
setup(
  name = 'keyness',
  packages = ['keyness'], # this must be the same as the name above
  version = '0.1',
  description = 'Calculates the log likelihood values of tokens in a corpus.',
  author = 'Mike Suhan',
  author_email = 'mikesuhan@gmail.com',
  url = 'https://github.com/mikesuhan/keyness', # use the URL to the github repo
  download_url = 'https://github.com/mikesuhan/keyness/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['log likelihood', 'statistics', 'corpora', 'corpus', 'linguistics'], # arbitrary keywords
  classifiers = [],
)